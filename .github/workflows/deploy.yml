cat > .github/workflows/deploy.yml <<'EOF'
name: Sanity CI

on:
  push:
    branches: [ "main" ]

jobs:
  sanity:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Echo
        run: echo "workflow yaml parses fine"
